---
phase: 04-add-dark-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/base.html
  - templates/series/*.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "Users can toggle between light and dark themes"
    - "Theme preference persists across browser sessions"
    - "All pages use the selected theme consistently"
  artifacts:
    - path: templates/base.html
      provides: "Theme CSS variables and toggle mechanism"
      min_lines: 200
    - path: templates/series/series_list.html
      provides: "Dark mode styling"
      min_lines: 50
  key_links:
    - from: base.html
      to: localStorage
      via: JavaScript
      pattern: "localStorage.setItem.*theme"
---

<objective>
Add dark mode functionality to Memoon with theme toggle and persistence.
</objective>

<context>
@.planning/PROJECT.md

# Current Template Structure
The base.html uses inline CSS with hardcoded colors:
- Background: #f5f5f5 (light gray)
- Text: #333
- Header: #2c3e50 (dark blue)
- Cards: white background with shadows
- Links: #2980b9 (blue)

# Requirements
1. Add CSS variables for theming
2. Add theme toggle button in header
3. Store preference in localStorage
4. Apply theme to all pages consistently
5. Default to system preference, allow override

# Implementation Approach
Use CSS custom properties (variables) for colors, JavaScript for toggle and localStorage persistence.
</context>

<tasks>

<task type="auto">
  <name>Add CSS variables and theme toggle to base.html</name>
  <files>templates/base.html</files>
  <action>
    1. Replace hardcoded colors with CSS custom properties (--bg-color, --text-color, --header-bg, --card-bg, --link-color, etc.)
    2. Add .dark-theme class with dark values
    3. Add theme toggle button in header with sun/moon icons
    4. Add JavaScript to:
       - Check localStorage for saved preference
       - Check system preference (prefers-color-scheme)
       - Toggle theme on button click
       - Save preference to localStorage
    5. Update the title tagline from "memory for cartoons" to "stories meet production assets"
  </action>
  <verify>
    <automated>grep -q "prefers-color-scheme" templates/base.html</automated>
  </verify>
  <done>Theme toggle appears in header, clicking toggles colors, preference persists after refresh</done>
</task>

<task type="auto">
  <name>Update series templates to use CSS variables</name>
  <files>templates/series/series_list.html,templates/series/series_detail.html,templates/series/season_detail.html,templates/series/episode_detail.html,templates/series/sequence_detail.html,templates/series/shot_detail.html,templates/series/character_list.html,templates/series/character_detail.html,templates/series/csv_import.html</files>
  <action>
    1. Add theme-specific CSS overrides where hardcoded colors exist
    2. Ensure all templates inherit base.html theming
    3. Test that dark theme applies consistently across all pages
  </action>
  <verify>
    <automated>ls templates/series/*.html | wc -l</automated>
  </verify>
  <done>All series templates render correctly in both light and dark themes</done>
</task>

</tasks>

<verification>
- Theme toggle visible in header
- Clicking toggle switches between light/dark
- Preference saved in localStorage
- Refreshing page maintains selected theme
- All pages (series, characters, import) show consistent theme
</verification>

<success_criteria>
Users can toggle dark mode, preference persists across sessions, all pages themed consistently
</success_criteria>

<output>
After completion, create `.planning/phases/04-add-dark-mode/04-01-SUMMARY.md`
</output>
