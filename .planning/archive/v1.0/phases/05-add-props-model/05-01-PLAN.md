---
phase: 05-add-props-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - series/models.py
  - series/admin.py
  - series/views.py
  - templates/series/shot_detail.html
autonomous: true
requirements: []
must_haves:
  truths:
    - "Props can be created and assigned to shots"
    - "Props display in shot detail page"
    - "Props are editable in admin"
  artifacts:
    - path: series/models.py
      provides: "Prop model with foreign key to Shot"
      min_lines: 30
  key_links:
    - from: shot_detail.html
      to: Prop model
      via: template context
      pattern: "props"
---

<objective>
Add a Prop model to track physical objects/items used in shots, similar to Character/Wardrobe pattern.
</objective>

<context>
@.planning/PROJECT.md

# Current Models
- Series, Season, Episode, Sequence, Shot
- Character, WardrobeItem (linked to shots via ShotCharacter)
- ShotCharacter (junction table with wardrobe)

# Requirements
1. Create Prop model (name, description)
2. Add Shot prop field (ManyToMany or through model)
3. Register in admin
4. Display in shot detail page
5. Create form for adding props to shots
</context>

<tasks>

<task type="auto">
  <name>Create Prop model</name>
  <files>series/models.py</files>
  <action>
    1. Add Prop model with:
       - name (CharField, max 200)
       - description (TextField, blank=True)
    2. Add ManyToManyField to Shot model for props
    3. Run makemigrations
  </action>
  <verify>
    <automated>grep -q "class Prop" series/models.py</automated>
  </verify>
  <done>Prop model created and migrations applied</done>
</task>

<task type="auto">
  <name>Register Prop in admin</name>
  <files>series/admin.py</files>
  <action>
    1. Import Prop model
    2. Add PropAdmin with list_display, search_fields
  </action>
  <verify>
    <automated>grep -q "@admin.register(Prop)" series/admin.py</automated>
  </verify>
  <done>Prop visible in admin interface</done>
</task>

<task type="auto">
  <name>Display props in shot detail</name>
  <files>templates/series/shot_detail.html</files>
  <action>
    1. Update ShotDetailView to include props in context
    2. Add props section to shot_detail.html template
  </action>
  <verify>
    <automated>grep -q "props" templates/series/shot_detail.html</automated>
  </verify>
  <done>Props display in shot detail page</done>
</task>

</tasks>

<verification>
- Prop model exists in database
- Can create props in admin
- Props display in shot detail
- Can assign props to shots
</verification>

<success_criteria>
Props can be created, assigned to shots, and displayed in the UI
</success_criteria>

<output>
After completion, create `.planning/phases/05-add-props-model/05-01-SUMMARY.md`
</output>
