---
phase: 06-add-user-roles-and-permissions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - series/models.py
  - series/admin.py
  - cartoon_manager/settings.py
  - cartoon_manager/urls.py
  - templates/base.html
  - templates/registration/login.html
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "Users can log in with username and password"
    - "Users can log out of their session"
    - "Users have a role (Admin, Power User, or User)"
    - "Role-based permissions restrict access to certain actions"
    - "UI shows user menu with login/logout options"
  artifacts:
    - path: "series/models.py"
      provides: "UserRole choices and UserProfile model with role field"
      min_lines: 10
      contains: "class UserProfile"
    - path: "templates/registration/login.html"
      provides: "Login form template"
      min_lines: 20
      contains: "{% csrf_token %}"
    - path: "templates/base.html"
      provides: "User menu dropdown in header-right"
      min_lines: 5
      contains: "{% if user.is_authenticated %}"
    - path: "cartoon_manager/urls.py"
      provides: "Login/logout URL routing"
      contains: "accounts"
  key_links:
    - from: "templates/base.html"
      to: "django.contrib.auth"
      via: "{% if user.is_authenticated %}"
      pattern: "user.is_authenticated"
    - from: "series/models.py"
      to: "django.contrib.auth.models.User"
      via: "OneToOneField"
      pattern: "UserProfile.*User"
---

<objective>
Add user authentication, roles, and permissions to Memoon.

Purpose: Enable user login/logout, assign roles (Admin, Power User, User), enforce role-based permissions, and add user menu to UI.

Output: Complete auth system with login/logout, role-based access control, and user menu in header.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@series/models.py
@templates/base.html
@cartoon_manager/settings.py
@cartoon_manager/urls.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create UserProfile model with role field</name>
  <files>series/models.py</files>
  <action>
    Add UserProfile model to series/models.py:
    - Create UserRole choices: ADMIN = 'admin', POWER_USER = 'power_user', USER = 'user'
    - Create UserProfile model with OneToOneField to User, role CharField with choices, default='user'
    - Add helper method has_role() to check if user has specific role
    - Add helper method is_admin() to check if user is admin

    Reference: Django's built-in auth system already in INSTALLED_APPS
  </action>
  <verify>
    <automated>python manage.py check</automated>
  </verify>
  <done>UserProfile model with role choices exists, linked to User model</done>
</task>

<task type="auto">
  <name>Task 2: Create authentication views (login/logout)</name>
  <files>series/views.py, series/urls.py, templates/registration/login.html</files>
  <action>
    Create authentication views in series/views.py:
    - login_view: LoginRequiredMixin, handles GET (show form) and POST (authenticate with django.contrib.auth)
    - logout_view: LogoutView, calls logout() then redirects to login
    
    Create series/urls.py entries:
    - path('accounts/login/', login_view, name='login')
    - path('accounts/logout/', logout_view, name='logout')
    
    Create templates/registration/login.html:
    - Extends base.html
    - Contains {% csrf_token %} form with username/password fields
    - Shows form.errors if any
    - Has "Login" submit button
  </action>
  <verify>
    <automated>python manage.py check</automated>
  </verify>
  <done>Login/logout views exist and route correctly</done>
</task>

<task type="auto">
  <name>Task 3: Add user menu to base template</name>
  <files>templates/base.html</files>
  <action>
    Update templates/base.html header-right section:
    - Add user menu dropdown next to theme toggle
    - Use Django auth context processor: {% if user.is_authenticated %}
    - When authenticated: Show username, Settings link (/admin/), Logout button (POST to /accounts/logout/)
    - When anonymous: Show Login link (/accounts/login/)
    
    Style the user menu with simple CSS (dropdown or inline links)
    Add login URL to settings: LOGIN_URL = '/accounts/login/'
  </action>
  <verify>
    <automated>python manage.py check</automated>
  </verify>
  <done>User menu shows login/logout options correctly based on auth state</done>
</task>

<task type="auto">
  <name>Task 4: Register UserProfile in admin and create migrations</name>
  <files>series/admin.py</files>
  <action>
    Register UserProfile in series/admin.py:
    - @admin.register(UserProfile)
    - List display: user, role
    - List filter: role
    - Search: user__username
    
    Run migrations:
    - python manage.py makemigrations series
    - python manage.py migrate
  </action>
  <verify>
    <automated>python manage.py migrate --plan && python manage.py check</automated>
  </verify>
  <done>UserProfile in admin, migrations applied successfully</done>
</task>

</tasks>

<verification>
- [ ] python manage.py check passes
- [ ] Login page renders at /accounts/login/
- [ ] Logout works at /accounts/logout/
- [ ] base.html shows user menu when logged in
- [ ] base.html shows login link when logged out
- [ ] UserProfile appears in admin interface
</verification>

<success_criteria>
Users can log in and out, roles are assigned via UserProfile, UI shows appropriate menu based on auth state, and role-based permissions can be enforced via has_role() method.
</success_criteria>

<output>
After completion, create `.planning/phases/06-add-user-roles-and-permissions/06-01-SUMMARY.md`
</output>
