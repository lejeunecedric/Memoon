---
phase: 07-storyboard-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - series/models.py
  - series/forms.py
  - series/views.py
  - series/urls.py
  - templates/series/episode_storyboard.html
  - templates/series/sequence_storyboard.html
  - templates/series/shot_form.html
  - templates/series/shot_detail.html
autonomous: true
requirements:
  - STORY-01
  - STORY-02
  - STORY-03
  - STORY-04

must_haves:
  truths:
    - "User viewing an Episode sees sequences displayed as visual cards in a responsive grid layout"
    - "User viewing a Sequence sees shots displayed as visual cards in a responsive grid layout"
    - "User can optionally upload an image file to any shot via the shot detail or edit interface"
    - "When a shot has an uploaded image, that image displays within the shot card in both Episode and Sequence view grids"
  artifacts:
    - path: "series/models.py"
      provides: "Shot model with image field"
      contains: "ImageField"
    - path: "series/views.py"
      provides: "EpisodeStoryboardView and SequenceStoryboardView"
      exports: ["EpisodeStoryboardView", "SequenceStoryboardView"]
    - path: "series/urls.py"
      provides: "URL routes for storyboard views"
      contains: "storyboard"
    - path: "templates/series/episode_storyboard.html"
      provides: "Grid display of sequences at episode level"
      min_lines: 40
    - path: "templates/series/sequence_storyboard.html"
      provides: "Grid display of shots at sequence level"
      min_lines: 50
  key_links:
    - from: "templates/series/episode_storyboard.html"
      to: "series/models.py"
      via: "sequence.shots.first.image.url"
      pattern: "\\.image\\.url"
    - from: "templates/series/sequence_storyboard.html"
      to: "series/models.py"
      via: "shot.image.url"
      pattern: "\\.image\\.url"
    - from: "series/views.py"
      to: "templates/series/episode_storyboard.html"
      via: "template_name"
      pattern: "episode_storyboard"
---

<objective>
Add visual storyboard display at Episode and Sequence levels with optional image upload per shot.

Purpose: Users can view sequences and shots as visual cards in grid layouts, with optional image upload and display.

Output: Shot image field, storyboard grid views/templates for Episode and Sequence levels.
</objective>

<execution_context>
@C:/Users/User/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

From series/models.py:
- Shot model exists with: sequence, number, script, characters (ManyToMany), props (ManyToMany), background, camera_angle, camera_movement, duration, notes

From templates/series/episode_detail.html:
- Shows sequences in card format with link to sequence detail

From templates/series/sequence_detail.html:
- Shows shots in table format

From templates/base.html:
- CSS .grid class: display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 20px;
- CSS .card class already exists for card styling
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image field to Shot model</name>
  <files>series/models.py, series/forms.py, series/admin.py</files>
  <action>
    1. Add to Shot model in series/models.py:
       - image = models.ImageField(upload_to='storyboards/', blank=True, null=True, help_text="Optional storyboard image")
    
    2. Update shot form in series/forms.py to include image field (create form if doesn't exist)
    
    3. Update shot_detail.html template to show image upload option and display existing image
       - Add file input for image upload in the detail/edit view
       - Display current image if present with proper alt text
    
    4. Run makemigrations and migrate to create database field
    
    Reference existing patterns from Shot model (line 118-145 in models.py).
  </action>
  <verify>
    <automated>python manage.py makemigrations series && python manage.py migrate</automated>
  </verify>
  <done>
    - Shot model has image field
    - Image can be uploaded via form
    - Image displays in shot detail view
  </done>
</task>

<task type="auto">
  <name>Task 2: Create storyboard grid views and templates</name>
  <files>series/views.py, series/urls.py, templates/series/episode_storyboard.html, templates/series/sequence_storyboard.html</files>
  <action>
    1. Add EpisodeStoryboardView to series/views.py:
       - Inherit from DetailView
       - Template: "series/episode_storyboard.html"
       - Context: episode with sequences.prefetch_related(shots)
    
    2. Add SequenceStoryboardView to series/views.py:
       - Inherit from DetailView  
       - Template: "series/sequence_storyboard.html"
       - Context: sequence with shots.all()
    
    3. Add URL patterns in series/urls.py:
       - episode/<int:pk>/storyboard/ -> EpisodeStoryboardView
       - sequence/<int:pk>/storyboard/ -> SequenceStoryboardView
    
    4. Create templates/series/episode_storyboard.html:
       - Use CSS grid layout (.grid class from base.html)
       - Display each sequence as a card
       - Show first shot's image as sequence thumbnail (or placeholder)
       - Include link to sequence storyboard view
       - Header with "Storyboard" title and "List View" link to episode_detail
    
    5. Create templates/series/sequence_storyboard.html:
       - Use CSS grid layout
       - Display each shot as a card
       - Show shot image if available, else show placeholder with shot number
       - Include shot details (script preview, characters)
       - Link to shot_detail for full details
       - Header with "Storyboard" title and "List View" link to sequence_detail
    
    Use existing card styling patterns from base.html (.card class) and episode_detail.html.
  </action>
  <verify>
    <automated>python manage.py check</automated>
  </verify>
  <done>
    - Episode storyboard view accessible at /episode/{id}/storyboard/
    - Sequence storyboard view accessible at /sequence/{id}/storyboard/
    - Grid displays sequences/shots as visual cards
    - Images display in shot cards when available
  </done>
</task>

</tasks>

<verification>
- [ ] Shot model has image field after migration
- [ ] Image upload works in shot form
- [ ] Episode storyboard shows sequences as grid of cards
- [ ] Sequence storyboard shows shots as grid of cards  
- [ ] Shot images display in storyboard grids
- [ ] Dark mode works with new templates
</verification>

<success_criteria>
- User viewing an Episode sees sequences displayed as visual cards in a responsive grid layout
- User viewing a Sequence sees shots displayed as visual cards in a responsive grid layout
- User can optionally upload an image file to any shot via the shot detail or edit interface
- When a shot has an uploaded image, that image displays within the shot card in both Episode and Sequence view grids
</success_criteria>

<output>
After completion, create `.planning/phases/07-storyboard-display/{phase}-01-SUMMARY.md`
</output>